#name
#on
#runs-on
#steps

name: Unit Tests

on: [push, pull_request]

jobs:
  backend-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r src/backend/requirements.pip

      - name: Run tests
        run: |
          export PYTHONPATH=$PYTHONPATH:$(pwd)/src/backend
          pytest src/backend/test/unit